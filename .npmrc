# npm configuration for SPM project

# Use exact versions to avoid dependency conflicts
engine-strict=true

# Allow peer dependency mismatches if needed
legacy-peer-deps=false

# Always use lockfile
prefer-frozen-lockfile=true

# Audit level for vulnerability detection
audit-level=moderate
